{
  "name": "web3-tools-monorepo",
  "private": true,
  "description": "Monorepo for web3-tools-mcp and its wallet relay",
  "license": "MIT",
  "workspaces": [
    "wallet-relay",
    "mcp"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm run build -w web3-wallet-relay && npm run build -w web3-tools-mcp",
    "test": "npm test -w web3-tools-mcp",
    "typecheck": "npm run typecheck -w web3-tools-mcp && npm run typecheck -w web3-wallet-relay",
    "lint": "biome check",
    "format": "biome check --write",
    "start": "npm start -w web3-tools-mcp",
    "start:wallet": "npm start -w web3-wallet-relay"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.14"
  }
}
